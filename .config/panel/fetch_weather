#/bin/sh

URL='http://www.accuweather.com/en/us/brooklyn-ny/11210/weather-forecast/4095_pc'

while :
do
    wget -q -O- "$URL" | awk -F\' '/acm_RecentLocationsCarousel\.push/{print "A" $2 ": "$16", "$12"°" }'| head -1 
    sleep 1
done
